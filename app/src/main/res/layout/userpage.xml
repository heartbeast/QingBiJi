<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#ffe5e5e5" >

    <ImageView
        android:id="@+id/userpage_listview_bg"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:contentDescription="@string/contentDescription"
        android:scaleType="fitStart"
        android:src="@drawable/pulllist_headbg" />

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:paddingTop="44dip"
        android:orientation="vertical" >

        <com.thinkernote.ThinkerNote.Other.PullListView
            android:id="@+id/userpage_pulllistview"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:cacheColorHint="#00000000"
            android:divider="@null"
            android:dividerHeight="0dip"
            android:fadingEdge="none"
            android:headerDividersEnabled="false" />

        <FrameLayout
            android:layout_width="fill_parent"
            android:layout_height="0dip"
            android:layout_weight="1"
            android:background="#ffe5e5e5" />
    </LinearLayout>
    <!-- tool bar -->

    <FrameLayout
        android:layout_width="fill_parent"
        android:layout_height="46dip"
        android:background="@drawable/toolbg" >

        <ImageButton
            android:id="@+id/userpage_home_btn"
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:layout_gravity="left"
            android:background="@drawable/btn_trans_selector"
            android:src="@drawable/logo" >
        </ImageButton>

        <ProgressBar
            android:id="@+id/userpage_progressbar"
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="right|center_vertical"
            android:layout_marginRight="10dip"
            android:visibility="gone" />
    </FrameLayout>

</FrameLayout>