<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" >

        <EditText
            android:id="@+id/netsearch_id_edittext"
            android:layout_width="0dip"
            android:layout_height="wrap_content"
            android:layout_marginLeft="10dip"
            android:layout_marginTop="10dip"
            android:layout_weight="1"
            android:hint="@string/netsearch_id_hint"
            android:imeOptions="actionSearch"
            android:inputType="number"
            android:maxLength="50"
            android:singleLine="true" >
        </EditText>

        <ImageButton
            android:id="@+id/netsearch_id_searchbtn"
            android:layout_width="48dip"
            android:layout_height="48dip"
            android:layout_marginTop="10dip"
            android:background="@drawable/btn_trans_selector"
            android:contentDescription="@string/contentDescription"
            android:src="@drawable/search_2" >
        </ImageButton>
    </LinearLayout>

    <TextView
        android:id="@+id/netsearch_id_hint"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_marginLeft="10dip"
        android:layout_marginRight="10dip"
        android:layout_marginBottom="10dip"
        android:singleLine="true"
        android:text="@string/netsearch_id" >
    </TextView>

</LinearLayout>