<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="44dip"
    android:background="@drawable/bottom_toolbg"
    android:orientation="horizontal" >

    <ProgressBar android:id="@+id/record_progressbar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="0dip"
        android:layout_height="3dip"
        android:layout_marginLeft="10dip"
        android:max="100"
        android:layout_weight="2"
        android:layout_gravity="center_vertical"
        android:progress="0" />
    
    <TextView android:id="@+id/record_time"
        android:layout_width="0dip"
        android:layout_height="fill_parent"
        android:gravity="center"
        android:textSize="14sp"
        android:layout_weight="1"
        android:textColor="#666666"
        android:text="00:00"/>

    <Button
        android:id="@+id/record_start"
        style="@style/button_grey"
        android:text="@string/noteedit_record_start"
        android:textSize="14sp" />

    <Button
        android:id="@+id/record_stop"
        style="@style/button_grey"
        android:layout_marginRight="5dip"
        android:text="@string/noteedit_record_stop"
        android:textSize="14sp" />

</LinearLayout>